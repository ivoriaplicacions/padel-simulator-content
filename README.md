# Catálogos de Padel Simulator

Este repositorio contiene únicamente documentos tácticos públicos y versionados. No contiene código de la aplicación, credenciales ni datos de jugadores.

La aplicación consulta `manifest.json`. Una versión nueva solo se anuncia después de publicar y comprobar el archivo completo indicado por `catalogUrl`.
